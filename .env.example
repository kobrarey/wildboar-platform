# App
SECRET_KEY=CHANGE_ME
EMAIL_PROVIDER=gmail_api

# Database (пример)
DATABASE_URL=postgresql://postgres:changeme@localhost:5432/WildBoar_platform

# SMTP (пример)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=notification@wildboar.finance
SMTP_PASSWORD=changeme
SMTP_FROM=Wild Boar <notification@wildboar.finance>

# Security codes
SECURITY_CODE_LENGTH=6
SECURITY_CODE_TTL_MINUTES=15
SECURITY_CODE_MAX_ATTEMPTS=5

SECURITY_CODE_RESEND_COOLDOWN_SECONDS=60

# BSC / QuickNode
# (на этапе генерации кошельков RPC не обязателен, но пусть переменная уже будет)
BSC_RPC_URL=https://your-quicknode-url

# Ключ для шифрования приватных ключей (генерируется один раз, не коммитить в git)
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
WALLET_ENC_KEY=CHANGE_ME_TO_FERNET_KEY

SECRET_KEY=секретный ключ
EMAIL_PROVIDER=gmail_api
EMAIL_FROM_EMAIL=notification@wildboar.finance
EMAIL_FROM_NAME=Wild Boar
BSC_WS_URL=wss://your-quicknode-wss-url
BSC_USDT_CONTRACT=0x55d398326f99059fF775485246999027B3197955
BSC_USDT_DECIMALS=18
BSC_CONFIRMATIONS=20
BSC_CONFIRM_POLL_SEC=15
BSC_BALANCE_POLL_SEC=15